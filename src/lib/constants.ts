/**
 * Global configuration constants for Chat with Website application.
 */

/** Maximum allowed pages to crawl per website (hard limit) */
export const MAX_PAGES = 15;

/** Default number of pages to crawl if unspecified */
export const DEFAULT_MAX_PAGES = 15;

/** Repository URL for support and contributions */
export const GITHUB_REPO_URL = "https://github.com/SavantYash/chat-with-website";
